// import style from './InfoTab.module.css'

const InfoTab = ({ children, className }) => {
    return (
        <div className={className}>
            {children}
        </div>
    )
}

export default InfoTab
